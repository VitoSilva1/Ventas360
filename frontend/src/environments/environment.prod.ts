export const environment = {
  production: true,
  /**
   * Google OAuth 2.0 Web Client ID para producción.
   * Reemplazar con el Client ID real en la pipeline de CI/CD o en la imagen de Docker.
   */
  googleClientId: 'TU_CLIENT_ID.apps.googleusercontent.com',
};
